class Solution {
    public int countTriplets(int[] arr) {
        int ans=0,x=0;
        for(int i=0;i<arr.length;i++){
            x^=arr[i];
            for(int j=i+1;j<arr.length;j++){
                x^=arr[j];
                if(x==0){
                    ans+=j-i;
                }
            }
            x=0;
        }
        return ans;
    }
}