class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int ans=0;
        int si=prices.size();
        int mini=prices[0];
        for(int i=0;i<si;i++){
            ans=max(ans,prices[i]-mini);
            mini=min(mini,prices[i]);
        }
        return ans;
    }
};