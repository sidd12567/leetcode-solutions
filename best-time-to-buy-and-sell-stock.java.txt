class Solution {
    public int maxProfit(int[] prices) {
        int r=Integer.MIN_VALUE;
        int ans=0;
        int pr=Integer.MAX_VALUE;
        for(int i:prices){
            if(i<pr){
                pr=i;
            }
            ans=Math.max(ans, i-pr);
        }
        return ans;
    }
}