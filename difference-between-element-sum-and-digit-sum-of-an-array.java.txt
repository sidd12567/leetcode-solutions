class Solution {
    public int differenceOfSum(int[] nums) {
        int ans=0;
        int tmp=0;
        for(int i:nums){
            ans+=i;
            while(i>0){
                tmp+=i%10;
                i=i/10;

            }
        }
        return Math.abs(ans-tmp);
        
    }
}