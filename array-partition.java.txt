class Solution {
    public int arrayPairSum(int[] nums) {
        int mn=0;
        Arrays.sort(nums);
        for(int i=0;i<nums.length-1;i+=2){
            mn+=Math.min(nums[i],nums[i+1]);
        }
        return mn;
    }
}