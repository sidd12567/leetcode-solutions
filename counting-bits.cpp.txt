class Solution {
public:
    vector<int> countBits(int n) {
        int ans=0;
        vector<int> a;
        for(int i=0;i<=n;i++){
            ans=__builtin_popcount(i);
            a.push_back(ans);
    }
        return a;
    }
};