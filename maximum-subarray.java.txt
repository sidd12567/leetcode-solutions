class Solution {
    public int maxSubArray(int[] nums) {
        int mx = Integer.MIN_VALUE;
        
        int ans=0;
        for(int i:nums){
            ans+=i;
            mx=Math.max(ans, mx);
            if(ans<0){
                ans=0;
            }
        }
        return mx;
    }
}