class Solution {
    public int missingNumber(int[] nums) {
        int ans=nums.length;
        Arrays.sort(nums);
        int l=nums.length;
        for(int i=0;i<l;i++){
            if(i!=nums[i]){
                ans=i;
                break;
            }
        }
        return ans;
    }
}