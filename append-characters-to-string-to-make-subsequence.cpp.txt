class Solution {
public:
    int appendCharacters(string s, string t) {
        int si=t.size();
        int ss=s.size();
        int i,j;
        for( i=j=0;i<ss&&j<si;i++){if(s[i]==t[j]){j++;}}
        return si-j;
    }
};